# Python / uv
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# Environment / secrets (.env only; keep .env.example committed)
.env
.env.*
!.env.example

# PaySim dataset + caches (large, kept local only)
PS_20174392719_1491204439457_log.csv
*.parquet

# Spark streaming lakehouse (bronze parquet + checkpoints)
data/

# Terraform (keep .terraform.lock.hcl and *.tf committed; gitignore the rest)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfplan
!terraform.tfvars.example

# Local logs / scratch
*.log

# dbt
dbt/target/
dbt/logs/
dbt/dbt_packages/
dbt/.user.yml

# Internal worklog + senior handover docs (never pushed to GitHub)
ourreadme.md
ourreadme
suggestionfixes.md
